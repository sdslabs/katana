apiVersion: v1
kind: Service
metadata:
  name: broadcast-service
spec:
  type: ClusterIP
  selector:
    app: {{.BroadcastLabel}}
  ports:
    - protocol: TCP
      port: {{.BroadcastPort}}
      targetPort: {{.BroadcastPort}}
