apiVersion: v1
kind: Service
metadata:
  name: broadcast-service
  namespace: katana
spec:
  type: ClusterIP
  selector:
    app: {{.BroadcastLabel}}
  ports:
    - protocol: TCP
      port: {{.BroadcastPort}}
      targetPort: {{.BroadcastPort}}
