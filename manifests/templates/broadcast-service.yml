apiVersion: v1
kind: Service
metadata:
  name: broadcast-service
spec:
  type: ClusterIP
  selector:
    deployment: {{.DeploymentLabel}}
    app: {{.BroadcastLabel}}
  ports:
    - protocol: TCP
      port: {{.BroadcastPort}}
      targetPort: {{.BroadcastPort}}
