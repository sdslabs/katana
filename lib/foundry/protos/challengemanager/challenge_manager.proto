syntax = "proto3";

package challengemanager;

message Request {
    string repo = 1;
    string uname = 2;
    string token = 3;
    string challname = 4;
}

message Response {
    bool success = 1;
}

service ChallengeFoundry {
    rpc Deploy(Request) returns (Response);
}
